# SafeVision dependencies
# Pinned to minor versions — patch updates are generally safe.
# Major pins on torch and ultralytics because their APIs change significantly.

# core ML
torch>=2.1,<3.0
ultralytics>=8.1,<9.0

# computer vision
opencv-python-headless>=4.8

# experiment tracking
wandb>=0.16

# dashboard
streamlit>=1.30
plotly>=5.18        # per-class recall chart on Training Results screen

# database
# sqlite3 is stdlib — no install needed

# video encoding — imageio-ffmpeg bundles its own ffmpeg binary (no system install needed).
# OpenCV's H.264 encoder is unreliable on most builds; mp4v + this re-encode is the reliable path.
imageio>=2.31
imageio-ffmpeg>=0.4.9

# utilities
python-dotenv>=1.0
numpy>=1.24

# dev / testing
pytest>=7.4
pytest-cov>=4.1
